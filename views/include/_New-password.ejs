<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudfare.com/ajax/libxs/font-awesome/5.15.4/css/all.min.css"> -->
    <!-- <link rel="stylesheet" href="../css/all.min.css"> -->
    <script defer src="../js/all.min.js"></script>
    <link rel="stylesheet" href="../css/nw.css">
    <link rel="stylesheet" href="../fonts/NetflixSansBlack.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansBold.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansIcon.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansLight.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansMedium.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansRegular.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansThin.ttf">
    <script  src="../js/newPass.js"></script>  
    <!-- <script defer src="../js/log.js"></script>   -->
    <!-- <script defer src="../js/reset.js"></script> -->
    <!-- <script src="../js/index.js"></script> -->
</head>
<body>
    <div class="logBox2">
        <form class="logcar2" id="emailForm" action="/change-password" method="post">
            <div class="ttl ttl2">Enter Password</div>
            <div class="logKit">  
                <div class="inputDiv">
                    <input type="password" id="password-input" name="newPassword" class="fnamei fbar" placeholder="Your password">
                    <button type="button" id="toggle-button" class="eyebtn" onclick="toggleit()"><i id="eye-icon"  class="fa fa-eye"></i></button>
                </div>
            </div>
            <div class="checkBox" id="disBox">
                <div class="ctext">Your password  must contain:</div>
                <div class="checkDot">
                    <div class="dot"><i id="dot1" class="fan fa fa-x"></i></div>
                    <div class="dotTExt" id="carac">A minimum of 8 characters</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot2" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac2">One upper  case letter</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot3" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac3">one lower case letter</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot4" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac4">One number</div>
                </div>
            </div>
            <div class="btnBox" id="appBox">
                <button type="submit" class="sigClick" id="sendEmailBtn">Save Password</button>
            </div>
        </form>
       
    </div>

   
    <!-- <div class="logBox2 logBox3">
        <form class="logcar2" action="/send-email" method="get">
            <div class="ttl ttl2">Enter Email</div>
            <div class="logKit">
                <label for="Email " id="" class="fname">Email</label>
                <input type="email" id="Email-input" name="userEmail" class="fnamei" placeholder="your email">
            </div>
            <div class="ttl ttl2">Enter reset code</div>
            <div class="inchuts">
                <input  class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
            </div>
            
            <div class="btnBox" id="appBox">
                <button type="submit" class="sigClick" >Reset</button>
            </div>
        </form>
    </div> -->
</body>

<!-- <script>
    document.addEventListener('DOMContentLoaded', function() {
        const emailInput = document.getElementById('Email-input');
        const resetButton = document.querySelector('.sigClick');

        // Initially disable the button
        resetButton.classList.add('disabled'); // Add a disabled class for styling

        // Function to check email validity
        function validateEmail(email) {
            const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            return re.test(String(email).toLowerCase());
        }

        // Enable/disable button based on email validity
        emailInput.addEventListener('input', function() {
            const isValidEmail = validateEmail(emailInput.value);
            if (isValidEmail) {
                resetButton.classList.remove('disabled');
                resetButton.removeAttribute('disabled');
            } else {
                resetButton.classList.add('disabled');
                resetButton.setAttribute('disabled', 'disabled');
            }
        });

        // Prevent link click if button is disabled
        resetButton.addEventListener('click', function(event) {
            if (resetButton.classList.contains('disabled')) {
                event.preventDefault(); // Prevent default link behavior
            }
        });
    });
</script> -->

</html>


            <!-- <div class="logKit">
                <label for="Email " id="" class="fname">Reset code</label>
                <input type="text" id="Email-input" class="fnamei" placeholder="Code">
            </div> -->
            <!-- <div class="logKit">
                <label for="password"  class="fname">New password</label>
                <div class="inputDiv">
                    <input type="password" id="password-input" class="fnamei fbar" placeholder="Your password">
                    <button id="toggle-button" type="button" class="eyebtn" onclick="toggleit()"><i id="eye-icon" class="fa fa-eye"></i></button>
                </div>
            </div>
            <div class="checkBox" id="disBox">
                <div class="ctext">Your password  must contain:</div>
                <div class="checkDot">
                    <div class="dot"><i id="dot1" class="fan fa fa-x"></i></div>
                    <div class="dotTExt" id="carac">A minimum of 8 characters</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot2" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac2">One upper  case letter</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot3" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac3">one lower case letter</div>
                </div>
                <div class="checkDot">
                    <div class="dot"><i id="dot4" class="fan fa fa-dot-circle"></i></div>
                    <div class="dotTExt" id="carac4">One number</div>
                </div>
            </div> -->