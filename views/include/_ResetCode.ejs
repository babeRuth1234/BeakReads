<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudfare.com/ajax/libxs/font-awesome/5.15.4/css/all.min.css"> -->
    <!-- <link rel="stylesheet" href="../css/all.min.css"> -->
    <script defer src="../js/all.min.js"></script>
    <link rel="stylesheet" href="../css/nw.css">
    <link rel="stylesheet" href="../fonts/NetflixSansBlack.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansBold.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansIcon.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansLight.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansMedium.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansRegular.ttf">
    <link rel="stylesheet" href="../fonts/NetflixSansThin.ttf">
    <!-- <script defer src="../js/log.js"></script>   -->
    <!-- <script src="../js/index.js"></script> -->
</head>
<body>
    <div class="logBox2">
        <div class="logcar2">
            <div class="ttl ttl2">Enter reset code</div>
            <div class="logKit">
                <!-- <form class="inchuts">
                    <input  class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                    <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                    <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                    <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric">
                </form> -->
                <form class="inchuts winger1" action="/verify-code" method="GET">
                    <div class="esees">
                        <input  class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric" name="digit1">
                        <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric" name="digit2">
                        <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric" name="digit3">
                        <input class="wingnut" type="text" maxlength="1" pattern="[0-9]" inputmode="numeric" name="digit4">
                    </div>
                    <button type="submit" class="sigClick extar">Continue</button>
                </form>
                
            </div>
            
        
        </div>
    </div>
</body>
<!-- <script>
    document.getElementById('verifyButton').addEventListener('click', async () => {
    const inputs = document.querySelectorAll('.wingnut');
    let verificationCode = '';
    inputs.forEach(input => {
        verificationCode += input.value;
    });

    const userEmail = ''; // Get the user's email (can be obtained from a previous form)

    try {
        const response = await fetch(`/verify-code?userEmail=${userEmail}&verificationCode=${verificationCode}`, {
            method: 'GET', // Assuming you're using a GET request
            // Add other headers or configurations as needed
        });

        const result = await response.text();
        alert(result); // Show a message based on the verification result
    } catch (error) {
        console.error('Error occurred:', error);
    }
});

</script> -->
</html>